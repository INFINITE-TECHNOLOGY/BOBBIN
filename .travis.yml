language: groovy
before_cache:
- rm -f $HOME/.gradle/caches/modules-2/modules-2.lock
cache:
  directories:
  - $HOME/.gradle/caches/
  - $HOME/.gradle/wrapper/
after_failure:
  - pwd
  - ls -l ./
  - ls -R ./
  - cat ./LOGS/BasicTest/ALL_LEVELS/BasicTest.log